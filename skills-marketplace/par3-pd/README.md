# par3-pd

**Tier:** 3rd party (`par3`)
**Source:** content creator **Patrick Dang** (`pd`)

Skills:

- `par3-pd:listbuilder` — build targeted outbound prospect lists.
- `par3-pd:offer-validator` — validate a business/service offer.

> Origin is encoded in the namespace on purpose. If you add more of Patrick Dang's skills, drop them
> in `skills/` here so they inherit the `par3-pd:` prefix. Skills from a *different* creator go in
> their own `par3-<source>/` plugin, never here.
